{
  "name": "invoicepro-kenya",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "npm --prefix frontend run build",
    "vercel-build": "npm --prefix backend install && npm --prefix frontend install --include=dev && npm --prefix frontend run build"
  }
}
